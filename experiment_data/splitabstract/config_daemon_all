export RUN_NAME="OMIM_ALL/splitabstract"
export ZEROBENCH_GOLD_STANDARD="omim_pubmed_profiles.txt"
export GOLD_STANDARDS="omim omim2 mondo" #export GOLD_STANDARDS="omim omim2 mondo"
export PREFILTER_PMIDS_FILE=""

export ADD_RANDOM_PAPERS="no"
export FILE_TO_GET_RANDOM=""

export METAREPORT_TAG="all.abstracts"
export METAREPORT_CASE="no_random_negatives" #with_random_negatives

export CORPUS_PATH="/mnt2/fscratch/users/pab_001_uma/pedro/software_data/pubmed"

export CHUNKSIZE=9
export PUBMED_ITEMS_PER_FILE=300000
export TEXT_BALANCE_SIZE=300000
export AFTER_FILTER_SIZE=300000

export SOFT_MIN_SIMILARITY=0.6
export HARD_MIN_SIMILARITY=0.7
export TOP_K=5

export ENGINE_AUTOFLOW_TEMPLATE="workflow.af"

export SPLIT_DOC="-s"
export DOC_TYPE="PubmedAbstract"
export EQUIVALENCES=""

export GPU_DEVICES="cuda:0-cuda:1-cuda:2-cuda:3-cuda:4-cuda:5-cuda:6-cuda:7"  #"cuda:0-cuda:1-cuda:2-cuda:3"
export N_GPUs="gres=gpu:"`echo $GPU_DEVICES | tr "-" "\n" | grep "cuda" | wc -l`
export USE_GPU_FOR_SIM_CALC=""

export TITLE_BLACKLISTED_WORDS="posters;abstracts;annual meeting;conference;congress;oral presentations"

#CREATE SEVERAL CORPUS BALANCED-FILLED FOLDERS TO DRISTIBUTE THE EMBEDDING AND SIM CALCULATION PROCESS IN SEVERAL NODES
export PARALLEL_FOLDERS_BASENAME="chunk"
export FOLDERS_TO_PARALLELIZE="${PARALLEL_FOLDERS_BASENAME}0;${PARALLEL_FOLDERS_BASENAME}1;${PARALLEL_FOLDERS_BASENAME}2;${PARALLEL_FOLDERS_BASENAME}3;${PARALLEL_FOLDERS_BASENAME}4;${PARALLEL_FOLDERS_BASENAME}5;${PARALLEL_FOLDERS_BASENAME}6;${PARALLEL_FOLDERS_BASENAME}7" #"${PARALLEL_FOLDERS_BASENAME}0;${PARALLEL_FOLDERS_BASENAME}1;${PARALLEL_FOLDERS_BASENAME}2"
export TSV_FOLDERS=`echo $FOLDERS_TO_PARALLELIZE | tr ";" "-"`
export PARALLEL_FOLDERS_ARRAY=(${FOLDERS_TO_PARALLELIZE//;/ })
export N_PARALLEL_FOLDERS=${#PARALLEL_FOLDERS_ARRAY[@]}


export BENCH_PACKAGE_SIZE=15