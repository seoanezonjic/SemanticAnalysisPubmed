export RUN_NAME="splitabstract_cosine"

export CORPUS_PATH="/mnt2/fscratch/users/pab_001_uma/pedro/software_data/pubmed"

export PUBMED_CHUNKSIZE=300000
export SOFT_MIN_SIMILARITY=0.6
export HARD_MIN_SIMILARITY=0.7
export TOP_K=5

export ENGINE_AUTOFLOW_TEMPLATE="workflow.af"

export SPLITTED="-s"
export PAPER=""
export EQUIVALENCES=""

export GPU_DEVICES="cuda:0-cuda:1-cuda:2-cuda:3-cuda:4-cuda:5-cuda:6-cuda:7"
export N_GPUs="gres=gpu:"`echo $GPU_DEVICES | tr "-" "\n" | grep "cuda" | wc -l`
export USE_GPU_FOR_SIM_CALC=""

export GOLD_STANDARDS="omim omim2 mondo"


#CREATE SEVERAL CORPUS BALANCED-FILLED FOLDERS TO DRISTIBUTE THE EMBEDDING AND SIM CALCULATION PROCESS IN SEVERAL NODES
export PARALLEL_FOLDERS_BASENAME="chunk"
#export FOLDERS_TO_PARALLELIZE="${PARALLEL_FOLDERS_BASENAME}0;${PARALLEL_FOLDERS_BASENAME}1;${PARALLEL_FOLDERS_BASENAME}2;${PARALLEL_FOLDERS_BASENAME}3;${PARALLEL_FOLDERS_BASENAME}4;${PARALLEL_FOLDERS_BASENAME}5;${PARALLEL_FOLDERS_BASENAME}6;${PARALLEL_FOLDERS_BASENAME}7;${PARALLEL_FOLDERS_BASENAME}8;${PARALLEL_FOLDERS_BASENAME}9"
export FOLDERS_TO_PARALLELIZE="${PARALLEL_FOLDERS_BASENAME}0;${PARALLEL_FOLDERS_BASENAME}1;${PARALLEL_FOLDERS_BASENAME}2"
export TSV_FOLDERS=`echo $FOLDERS_TO_PARALLELIZE | tr ";" "-"`
export PARALLEL_FOLDERS_ARRAY=(${FOLDERS_TO_PARALLELIZE//;/ })
export N_PARALLEL_FOLDERS=${#PARALLEL_FOLDERS_ARRAY[@]}


export BENCH_PACKAGE_SIZE=50